/**
 * @vue-flow/core 的防腐层。原生 API 只允许在本文件内出现，业务层只认下方导出的我方契约。
 */

export function mountFlowGraph(_container: HTMLElement): void {
  throw new Error('not implemented') // TODO(skeleton): 挂载 @vue-flow/core
}
